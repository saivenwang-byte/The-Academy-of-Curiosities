# 三稿对比审读 UI · V0.1

> **日期**：2026-06-08  
> **入口**：`正式版/06_审读对比UI/index.html`  
> **数据脚本**：`03_故事内容/tools/build_comparison_ui_data.py`

---

## 1. 版本映射（UI 内展示）

| 标签 | 含义 | Git 来源 |
|------|------|----------|
| **原稿** | Pre–global-IP R16（R15 baseline） | `050f810` |
| **修改1稿** | A001-only R16 wave | `a374cb7` |
| **修改2稿** | 五案 R16 + H/C/S | `b0adc91` |

- **A001**：三稿均不同（R16 分两波：先 A001，后 A002–A005）
- **A002–A005**：原稿 = 修改1稿；修改2稿 = R16

---

## 2. 打开方式

### 推荐（file:// 可直接用）

双击或在浏览器打开：

```
file:///D:/【AI Project】/【The Academy of Curiosities】/03_故事内容/第1卷_觉得奇怪就先观察/正式版/06_审读对比UI/index.html
```

UI 通过同目录 `comparison_data.js` 加载数据（避免 file:// 下 fetch JSON 的 CORS 限制）。

### 可选：本地 HTTP

```powershell
cd "03_故事内容/第1卷_觉得奇怪就先观察/正式版/06_审读对比UI"
python -m http.server 8765
# 浏览器 http://localhost:8765
```

---

## 3. 重新生成数据

正文或打分更新后，在仓库根目录执行：

```powershell
python "03_故事内容/tools/build_comparison_ui_data.py"
```

输出：

| 文件 | 用途 |
|------|------|
| `06_审读对比UI/comparison_data.json` | 机器可读 SSOT · diff/段落/分数 |
| `06_审读对比UI/comparison_data.js` | 浏览器 `file://` 加载用 |

数据来源：

- 三稿正文：`git show` @ `050f810` / `a374cb7` / `b0adc91`
- 打分：`V2迁移/scores_mvp_latest.json`
- 修改摘要：`*_R16_globalIP.md` + `37_A001_R16_原稿对比表_V0.1.md`（A001）

---

## 4. UI 功能

1. **案 Tab** A001–A005
2. **三列并排**：原稿 | 修改1稿 | 修改2稿 · 可同步滚动
3. **打分面板**（可折叠）：文学卷专家、机制/科学、H/C/S before→after、读者群、E20 探针占位
4. **修改内容面板**：R16 维度表 + M1–M4 标签
5. **段落高亮**：改1 vs 原稿、改2 vs 改1 差异段

---

## 5. 维护

- 新 R17 轮次：更新脚本内 `COMMIT_DRAFT*` 常量或加 CLI 参数
- E20 真实试读完成后：更新 `scores_mvp_latest.json` 后重跑脚本
