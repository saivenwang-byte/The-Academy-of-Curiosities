---
description: Git 与正典索引 — 改 docs/正典后更新索引，规范 commit
alwaysApply: true
---

# Git 与索引

**改以下路径后**，检查是否需更新 `00_项目总览/正典文件索引.md` 或 `docs/00_AGENT_CAPABILITY_MAP.md`：

- `docs/` · `02_/` · `skills/` · `.cursor/rules/` · `00_项目总览/`

**Commit 前**：`git status` · 不提交 `.env`/密钥 · 不 force push main · 仅用户要求时 commit/push

**Message 风格**：`Add …` / `Update …` / `Fix …` + 卷名或模块（英文或中文均可，一句说清 why）

**勿**：重复创建同名正典；在 `05_创作对话记录/` 当正典用。
