# 分支与基线执行记录

> 日期：2026-06-07  
> 状态：`HISTORICAL_RECORD · EXPLICIT_AUTH_REQUIRED`

## 1. 当前裁决

此前“单主线＋目录隔离”的技术方案仅作为历史记录保留，不再自动执行。

自2026-06-07最新授权规则起：

- 默认聊天内容不关联GitHub；
- 允许只读检查仓库；
- 修改、创建、删除、提交、分支、PR、Issue或CI操作必须获得IP Owner明确授权；
- “开始”“继续”“执行”不等于GitHub写入授权；
- 每次授权只覆盖当次明确任务。

## 2. 分支与基线

| 对象 | 用途 | 当前规则 |
|---|---|---|
| `main` | 当前生产主线 | 未获明确授权不得写入 |
| `baseline/v1-narrative-20260607` | V1历史基线 | 只读、用于比较与回退 |
| `reframe/ip-v2-campus-ripple` | 历史迁移快照 | 冻结，不再作为持续同步目标 |

## 3. V2文件状态

在MIG-G4前，V2文件只允许使用：

- `MIGRATION_CANDIDATE`
- `INTERNAL_REVIEW`
- `PROTOTYPE`
- `READER_TEST_PENDING`
- `SUPERSEDED`
- `ARCHIVED`

不得因文件位于`main`而自动视为正典。

## 4. 当前Gate

统一为：

> `MIG-G1结构候选 / MIG-G2内部原型进行中`

MIG-G3真实读者验证未完成；MIG-G4正典迁移未授权、未发生。

## 5. 当前权威治理文件

见：

`V2_治理状态纠偏_20260607.md`

## 6. 当前出口

`EXPLICIT_AUTH_GOVERNANCE_ACTIVE`
